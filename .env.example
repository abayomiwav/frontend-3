# The StellarExpress backend's GraphQL endpoint. The sign-in and sign-up
# pages (/signin, /signup) call this directly. Defaults to a local dev
# backend if unset — see https://github.com/StellarExpress/backend.
NEXT_PUBLIC_API_URL="http://localhost:4000/graphql"
